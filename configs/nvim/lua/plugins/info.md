---
title: Plugins
description: One lazy spec per file. Auto-imported. Oil must not steal <C-s>.
date created: 2026-08-16
date modified: 2026-08-17
---

# Plugins
@index.md

One `lazy.nvim` spec per file, named after the plugin/feature. `core.lazy` imports this whole dir. No register.

Oil: keep `<C-h>`, `<C-s>`, `<C-t>` disabled (`<C-s>` is global save). Surround: visual `S` stays Flash. Colors come from `require("shared.colors")`, not hex in the spec.
